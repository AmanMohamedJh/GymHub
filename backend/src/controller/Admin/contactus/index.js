const getContactMessages = require("./getContactMessages");
const deleteContactMessage = require("./deleteContactMessage");
const replyToContactMessage = require("./replyToContactMessage");

module.exports = {
  getContactMessages,
  deleteContactMessage,
  replyToContactMessage,
};
